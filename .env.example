# Footage → Tactics — copy to .env and fill in. Never commit .env.

# --- Supabase (per-club DB + heatmap storage) ---
SUPABASE_URL=https://tifynfruqgkbuiyiljfb.supabase.co
SUPABASE_SERVICE_KEY=

# --- Anthropic (the analyst, workflow node 8) ---
ANTHROPIC_API_KEY=

# --- CV service (workflow node 3) ---
# In the hackathon build this points at the mock CV n8n webhook.
# Swap for the Modal/RunPod endpoint at Rung 3 — the §6b contract is unchanged.
CV_SERVICE_URL=https://shaneesilva.app.n8n.cloud/webhook/cv-mock
MOCK_MODE=true

# --- n8n webhooks the frontend calls ---
N8N_SUBMIT_URL=https://shaneesilva.app.n8n.cloud/webhook/match-submit
N8N_REPORT_URL=https://shaneesilva.app.n8n.cloud/webhook/report
N8N_REPORT_HTML_URL=https://shaneesilva.app.n8n.cloud/webhook/report-html

# --- Optional (Rung 3 / R2 bonus) ---
VEO_API_TOKEN=
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=

# NOTE: N8N_MCP_TOKEN (for Claude Code's MCP connection) is NOT read from here.
# It is referenced by ../.mcp.json and must be exported in your shell.
